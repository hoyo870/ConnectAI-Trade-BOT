# ConnectAI 매매 전략 프레임워크

> 최종 업데이트: 2026-06-01  
> 5분봉 기반 자동화 트레이딩 (DL 신호 + 선택적 규칙 게이트)

---

## 핵심 원칙

1. **코인별 독립 모델**: BTC 매매 → BTC 신호기, ETH 매매 → ETH 신호기 (신호 교차 사용 금지)
2. **DL 우선**: DL 신호만으로 pos≥6/10 달성 시 게이트 없이 운용
3. **게이트는 보완재**: DL이 방향을 잘 못 잡는 코인에만 규칙 게이트 추가
4. **파라미터 분리**: 진입 방향(게이트) ↔ 레버리지/청산 강도(DL 신호) 역할 구분

---

## Layer 1 — DL 신호 (모든 코인 공통)

### 모델 구조
- **아키텍처**: TCN(Temporal Convolutional Network) + Multi-Head Self-Attention
- **출력**: `signal_long`, `signal_short`, `signal_context` ∈ [0, 1]
- **입력**: 43개 피처 (OHLCV + EMA/MACD/RSI/ATR/BB 등)
- **훈련 분할**: train 90% / val 10% (최신 데이터 최대 학습)

### 4-Tier 레버리지 구조
신호 강도에 따라 레버리지 자동 결정:

| 티어 | 신호 임계값 | 레버리지 |
|------|------------|---------|
| T1 (최강) | ≥ 0.72 | 10× |
| T2 | ≥ 0.62 | 4× |
| T3 | ≥ 0.55 | 3.5× |
| T4 | ≥ 0.50 | 2.5× |

- `sig_upper_thr=0.65` 이상 신호는 역방향 청산 트리거로도 사용
- 신호 강도 낮은 구간(≤0.50)은 진입하지 않음

---

## Layer 2 — 규칙 게이트 (선택적, 필요한 코인만)

원래 5분봉 단타 수동 전략의 자동화 버전. DL 신호 방향이 충분히 신뢰되지 않을 때 진입 방향을 규칙으로 검증.

### 롱(Long) 게이트 조건

| 조건 | 내용 |
|------|------|
| MA 크로스 | EMA9 > EMA21 (골든크로스 상태) |
| RSI 범위 | RSI ≥ 50 AND RSI ≤ `rsi_long_hi` (기본 60) |
| 거래량 | 현재 거래량 > EMA20 거래량 × `vol_mult` (기본 0.8) |
| 1h 추세 | 1시간봉 EMA20 기준 상승 추세 (선택적) |

### 숏(Short) 게이트 조건

| 조건 | 내용 |
|------|------|
| MA 크로스 | EMA9 < EMA21 (데드크로스 상태) |
| RSI 범위 | RSI ≤ 50 AND RSI ≥ `rsi_short_lo` (기본 30) |
| 거래량 | 현재 거래량 > EMA20 거래량 × `vol_mult` (기본 0.8) |
| 1h 추세 | 1시간봉 EMA20 기준 하락 추세 (선택적) |

### 게이트 적용 규칙
- **신규 진입에만 적용**: 게이트가 열린 방향으로만 새 포지션 진입
- **청산/역추세는 적용 안 함**: 청산 타이밍은 DL 신호 원본 그대로 사용 (숏 청산이 게이트에 의해 막히는 문제 방지)

---

## 코인별 전략 결정 기준

| 코인 | 게이트 | 이유 |
|------|--------|------|
| **BTC** | ❌ 없음 | DL 단독 pos=6/10, +180.7% — 충분 |
| **ETH** | ✅ 적용 | DL 단독 pos=4/10, -81.2% — 하락장 숏 포착 불가 |
| **SOL** | 미정 | Phase 3에서 DL 단독 테스트 후 결정 |
| **XRP** | 미정 | Phase 4에서 결정 |

### Phase 3+ 신규 코인 판단 기준
1. DL 모델 훈련 → 독립 신호 생성
2. 게이트 없이 10창 롤링 백테스트
3. **pos_cnt ≥ 6/10** → 게이트 없이 운용
4. **pos_cnt < 6/10** → ETH 동일 게이트 템플릿으로 파라미터 탐색

---

## 현재 코인별 파라미터

### BTC (Phase 1 완료)

| 파라미터 | 값 |
|----------|----|
| 전략 | 순수 DL |
| 티어 rr 배율 | ×1.15 |
| price_sl | 2% |
| price_tp | 6% |
| min_hold | 120봉 (10시간) |
| max_dd_cb | 1.0 |
| 파라미터 파일 | `models/production/btc_params.json` |

| 지표 | 2026 | 역사 10창 |
|------|------|---------|
| 수익률 | +180.7% | pos=6/10 |
| WR | 49.6% | avgWR=45.9% |
| TPD | 1.85 | avgTPD=2.04 |

### ETH (Phase 2 완료)

| 파라미터 | 값 |
|----------|----|
| 전략 | 하이브리드 (게이트 + DL) |
| 게이트 | EMA9/21 + RSI60/30 + Vol×0.8 + 1h |
| 티어 rr 배율 | ×0.90 |
| price_sl | 2% |
| price_tp | 15% |
| min_hold | 60봉 (5시간) |
| max_dd_cb | 1.0 |
| 파라미터 파일 | `models/production/eth_params.json` |

| 지표 | 2026 | 역사 10창 |
|------|------|---------|
| 수익률 | +60.9% (ETH -32% 하락장) | pos=7/10 |
| WR | — | avgWR=46.6% |
| TPD | — | avgTPD=2.53 |
| L/S 비율 | 96L/134S (숏 58%) | — |

---

## SL/TP 설계 원칙

현재: **고정 퍼센트** (price_sl/tp)  
Phase 3 검토: **ATR 배수** (atr_sl = atr × N)

| 방식 | 장점 | 단점 |
|------|------|------|
| 고정 % | 단순, 예측 가능 | 변동성 무시 |
| ATR 배수 | 시장 변동성 반영 | 파라미터 추가 |

SOL은 변동성이 BTC/ETH보다 높으므로 ATR 방식 우선 검토.

---

## 역사적 검증 방법

- **창 수**: 10창 (seed=42, 3개월 단위)
- **구간**: 코인 데이터 시작 ~ 현재
- **통과 기준**: pos_cnt≥6, avgWR≥45%, avgTPD≥1.5
- **구조적 실패 창 (파라미터로 해결 불가)**:
  - BTC: ATH crash(2021), Luna(2022), FTX(2022)
  - ETH: ATH 랠리(2021), BTC ETF 랠리(2024)
